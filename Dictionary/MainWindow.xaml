<Window x:Class="RedBlackTreeDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RedBlackTreeDemo"
        Title="Dictionary" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <TextBlock Text="Search: " VerticalAlignment="Center" Margin="0,0,10,0" />
            <TextBox x:Name="txtSearch" Width="200" />
            <Button Content="Search" Click="OnSearchClicked" Margin="10,0" />
            <Button Content="Random Word" Click="OnRandomWordClicked" Margin="10,0" />
        </StackPanel>

        <ListBox x:Name="lstDefinitions" Grid.Row="1" Margin="10" />

    </Grid>
</Window>